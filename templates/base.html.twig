<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %} {{'Welcome!'|upper}} {% endblock %}</title>
        
        {#Liens css#}
        <link rel="stylesheet" href="{{asset('assets/css/bootstrap.min.css')}}">
        <link rel="stylesheet" href="{{asset('assets/fonts/fontawesome.min.css')}}">
        <link rel="stylesheet" href="{{asset('assets/fonts/all.min.css')}}">
        {% block stylesheets %}{% endblock %}
        <link rel="stylesheet" href="{{asset('assets/css/styles.css')}}">
        
        {#Liens Js#}
        <script src="{{asset('assets/js/bootstrap.min.js')}}" defer></script>
        <script src="{{asset('assets/fonts/fontawesome.min.js')}}" defer></script>
        <script src="{{asset('assets/fonts/all.min.js')}}" defer></script>
        {% block javascripts %}{% endblock %}
        <script src="{{asset('assets/js/bootstrap.bundle.min.js')}}" defer></script>
    </head>
    <body>
     <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
        <div class="container-fluid">
            <a class="nav-link active navbar-brand" href="{{path('app_home')}}">{{'techlab'|upper}}
                <span class="visually-hidden">(current)</span>
            </a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarColor01">
            <ul class="navbar-nav me-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{path('app_create')}}">Form</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="#">About</a>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" data-bs-toggle="dropdown" href="#" role="button" aria-haspopup="true" aria-expanded="false">Catégories</a>
                    <div class="dropdown-menu">
                        <a class="dropdown-item" href="#">Frameworks</a>
                        <a class="dropdown-item" href="#">Langages de Programmation</a>
                        <a class="dropdown-item" href="#">Bases de Données</a>
                        <a class="dropdown-item" href="#">Modélisation de Données</a>
                        <div class="dropdown-divider"></div>
                        <a class="dropdown-item" href="#">Webmaster</a>
                    </div>
                    </li>
            </ul>
            <form class="search d-flex w-50">
                <input class="form-control" type="search" placeholder="Search">
                <button class="btn btn-primary" type="submit">
                <i class="fa fa-search-plus" aria-hidden="true"></i></button>
            </form>
             <div class="navbar-nav">
                    <a href="{{path('app_login')}}" class="votre-compte ms-3 text-white">Votre Compte</a>
            </div> 
            <div class="navbar-nav ms-2">
           <a href="#"><i class="fa fa-cart-plus fa-2x"  style="color: white;"></i></a>
           </div>
        </div>
    </nav>

    <main class="container">
        {% block body %}{% endblock %}
    </main>
    </body>
</html>
